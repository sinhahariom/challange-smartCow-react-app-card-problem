import {createContext} from 'react';

export const BroadCastContext = createContext({});           
{/*This is a simple Broadcasting Context made for passing state between Parent 'Body Wrapper' and its childs 'LeftNav' and 'Right Section'*/}
